# 专利撰写 VBA 项目交接说明

这份文件用于交给另一台电脑上的 Codex。请先阅读本文件和 `README.md`，再开始修改。

## 项目位置与当前状态

- GitHub 仓库：[Wujiamu/PatentTools-VBA](https://github.com/Wujiamu/PatentTools-VBA)
- 远程仓库为私有仓库，默认分支为 `main`。
- 当前基线提交：`0de563f`。
- 当前工作区在交接前应保持干净；先运行 `git status` 核对。
- 本机 `AGENTS.md` 是 Codex 协作说明，已被 `.gitignore` 忽略，不在 GitHub 中。不要重新添加、提交或上传它。

## 用户目标

项目用于中文机械领域专利申请文件撰写，后续工作有两条主线：

1. 修复现有 VBA 的 BUG，使处理规则更符合真实专利撰写场景。
2. 将 Word 宏列表中的多个中文入口收敛为一个入口，由工具面板调用各项功能。

## 已完成内容

已从本机 Word 启动模板中提取宏名为中文的六个专利撰写工具，并排除了 CAD 插件的英文宏：

- `权利要求标号`
- `权利要求转发明内容`
- `具体实施方式标号`
- `页眉双模式同步`
- `一键改格式_全文修改_不兼容修订`
- `一键改格式_选中部分修改_兼容修订`

源码层面已经完成第一版入口重构：

- `src/modToolboxEntry.bas`：唯一公开入口 `专利撰写工具箱`。
- `src/modToolDispatcher.bas`：根据工具 ID 调用旧功能。
- `src/frmPatentToolbox.frm`：第一版无模式工具面板。
- 六个旧模块增加了 `Option Private Module`，使其不再作为 Word 宏列表中的公开入口。
- 原始六个功能的业务逻辑暂时没有重写，面板先调用原实现。

## 重要未完成事项

目前的“合并”主要是源码层合并。`dist/PatentTools.dotm` 仍是从 Word 启动目录复制的原始基线模板，不是已经导入新面板源码的发布模板。因此不要向用户声称新的合并版 `.dotm` 已经可以直接安装。

后续需要在测试模板副本中：

1. 用 VBE 导入 `src/` 中的模块和窗体，或完善自动导入/构建流程。
2. 验证中文过程名、窗体标题、唯一入口和六个面板按钮。
3. 对比结构重构前后的输出，确认只改变入口，不改变原有业务行为。
4. 生成版本化发布模板，例如 `dist/PatentTools-0.2.0.dotm`，不要覆盖原始基线。

## 另一台电脑的开始步骤

在另一台电脑上执行：

```powershell
git clone https://github.com/Wujiamu/PatentTools-VBA.git
Set-Location .\PatentTools-VBA
git status
Get-Content .\README.md
Get-Content .\HANDOFF.md
```

然后确认：

- 已安装桌面版 Word，并记录 Word 版本及 32/64 位。
- 当前使用的是测试用 `.dotm` 副本，不是 Word `STARTUP` 正式模板。
- 已备份正式模板；开发过程中不要直接覆盖正在使用的模板。
- Word 的“信任对 VBA 项目对象模型的访问”如果未开启，采用手工导入，不要自行修改安全设置。

准备 VBE 导入副本：

```powershell
.\scripts\prepare_vbe_import.ps1
```

导入后重点检查：

- `专利撰写工具箱` 是唯一公开宏入口。
- 六个旧中文入口可以由分发器调用，但不出现在普通宏列表中。
- 中文代码页转换没有造成中文过程名或窗体标题乱码。
- 无模式面板打开后，点击按钮时重新读取当前文档和选区。

## 测试与开发顺序

不要一开始同时修改入口结构和业务规则。建议按以下顺序：

1. 先记录六个旧宏的基线行为和已知问题。
2. 在测试副本中验证入口重构前后输出一致。
3. 为每一个 BUG 建立最小可复现样例，记录实际结果和期望结果。
4. 优先提取纯文本规则，减少对 `ActiveDocument`、`Selection` 和全局查找替换的直接依赖。
5. 逐项修复并回归测试，不要把互不相关的规则调整堆在同一提交。
6. 最后进行 Word 集成测试和人工验收。

重点测试范围：修订模式、批注、域、超链接、内容控件、表格、公式、多节页眉、首页/奇偶页不同、名称包含关系、重复附图标记、网址邮箱和中英文混合格式。

## 明确排除项

不要把以下 CAD 相关英文模块纳入本项目：`PatentExtractor`、`AutoExport`、`DictModel`、`JsonWriter`、`Patterns` 等。不要修改或替换用户正在使用的正式 Word 启动模板。

## 推送约定

每次准备 `git push` 前必须检查 README：

- 功能、入口、目录、构建方式或发布状态变化时，必须在同一次提交中更新 `README.md`。
- 即使判断无需更新，也要在推送前完成检查。
- `AGENTS.md` 只保留在本机，不得提交到 GitHub。

## 交接完成标准

接手工作只有在以下内容清楚后才算完成：

- 能在测试模板副本中导入并打开工具面板。
- 能说明六个工具各自的输入范围和修订模式行为。
- 有标准样例、边界样例和 BUG 回归样例。
- 出错后 Word 的 `TrackRevisions`、`ScreenUpdating`、`StatusBar` 等状态能够恢复。
- 新的发布模板与源码、README、测试记录对应到明确的 Git 提交。
