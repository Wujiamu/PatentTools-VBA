# 专利撰写 VBA 项目开发与协作指南

## 1. 项目目标

本项目维护一组用于中文专利申请文件撰写的 Word VBA 工具，目标分为两条相互关联但应分阶段实施的工作线：

1. 修复现有宏的 BUG，并使处理规则更贴合真实的专利撰写场景。
2. 将 Word“宏”列表中的多个入口收敛为一个入口，由该入口打开工具面板，再从面板选择具体工具。

当前基线包含以下六个用户可见宏：

- `权利要求标号`
- `权利要求转发明内容`
- `具体实施方式标号`
- `页眉双模式同步`
- `一键改格式_全文修改_不兼容修订`
- `一键改格式_选中部分修改_兼容修订`

英文的 CAD 相关宏不属于本项目。不要把 `Normal.dotm` 中的 `PatentExtractor`、`AutoExport`、`DictModel`、`JsonWriter`、`Patterns` 等模块合并进本项目。

## 2. 总体实施路线

建议按以下顺序推进，避免“结构重构”和“规则修改”互相干扰。

### 阶段 A：冻结并描述当前行为

- 保留当前 `dist/PatentTools.dotm` 和初始 Git 提交，作为可回退基线。
- 为每个工具写清输入范围、前置条件、修改内容、修订模式行为、错误提示和预期结果。
- 用现有模板运行第一组合成样例，保存修改前文件、修改后文件和观察结果。
- 收集已知 BUG，并将每个 BUG 转化为可重复的最小样例。

本阶段不修改业务逻辑。它的产物是“现状说明 + 可复现样例”，用于判断后续改动是否意外改变行为。

### 阶段 B：只做入口与结构重构

- 新增唯一公开入口宏，例如 `专利撰写工具箱`。
- 入口打开一个 Word VBA `UserForm` 工具面板。
- 将六个现有入口改为内部可调用过程，并使用 `Option Private Module` 隐藏在 Word 宏列表中。
- 面板按钮暂时调用原有实现，不在同一阶段修改规则。
- 对比阶段 A 的样例，确认重构前后输出一致。

这一阶段完成后，Word 的宏列表原则上只显示 `专利撰写工具箱`。

### 阶段 C：逐项修复 BUG 和调整撰写规则

- 一个 BUG 或一条规则调整对应一个最小复现样例。
- 先记录当前错误结果和期望结果，再修改代码。
- 每次修改至少验证对应样例，并抽查标准样例，防止回归。
- 不把互不相关的规则调整堆在同一个提交中。

### 阶段 D：集成验收与发布

- 在专门的测试模板和文档副本中运行全部工具。
- 验证 Word 版本、32/64 位、修订模式、不同节和页眉设置等兼容性。
- 生成新的版本化 `.dotm`，保留旧版本用于回退。
- 经确认后再替换 Word `STARTUP` 目录中的正式模板；日常开发不得直接改正式模板。

## 3. 建议的目标结构

### 3.1 单一入口

建议新增标准模块 `modToolboxEntry`，其中只保留一个无参数公开过程：

```vb
Public Sub 专利撰写工具箱()
    frmPatentToolbox.Show vbModeless
End Sub
```

工具面板建议使用无模式窗口 `vbModeless`，这样用户可以在面板打开后继续切换文档或重新选择文本。每次点击按钮时必须重新读取并校验当前文档和选区，不能假定打开面板时的选区仍然有效。

### 3.2 面板第一版

第一版面板以稳定、清楚为主，不必追求复杂视觉效果。建议包含：

- 当前活动文档名称和当前选区状态。
- “标号处理”分组：权利要求标号、具体实施方式标号。
- “内容转换”分组：权利要求转发明内容。
- “文档信息”分组：页眉双模式同步。
- “格式清理”分组：全文清理、选区清理。
- 每个工具旁的一行简短说明，例如“需要先选中权利要求区域”。
- 状态栏或结果区域，显示执行数量、跳过数量和警告，而不只显示笼统的“完成”。

对全文清理、关闭修订后修改等影响较大的动作，面板应明确显示作用范围和修订处理方式，并在执行前进行确认。

### 3.3 内部代码分层

VBA 不必引入过度复杂的设计模式，但应把下列职责分开：

- `modToolboxEntry`：唯一公开入口。
- `frmPatentToolbox`：界面和按钮事件，不承载文本处理算法。
- `modToolDispatcher`：根据工具编号调用对应功能，统一前置检查和结果处理。
- `modDocumentContext`：活动文档、选区、正文范围、修订状态等上下文校验。
- `modFigureTagService`：统一解析“附图标记说明”和附图标号，供权利要求与具体实施方式共同使用。
- `modFormatService`：统一格式清理的公共步骤，参数决定全文/选区及修订策略。
- 各功能模块：只保留功能特有规则。
- `modOperationGuard`：统一保存并恢复 `TrackRevisions`、`ScreenUpdating`、`StatusBar` 等应用状态。
- `modResult` 或轻量结果对象：返回修改数、跳过数、警告和错误信息。

当前代码中以下重复应优先收敛：

- 两个标号模块重复的标号字典解析、排序、后缀判断逻辑。
- 两个格式模块重复的字体、隐形字符、Markdown、标点和引号处理逻辑。
- 各入口重复的错误处理、状态保存和消息框逻辑。

## 4. 样例与测试材料

### 4.1 推荐采用混合样例策略

合成样例和真实样例用途不同，建议两者都准备：

- 合成样例由本项目生成，内容不涉密，专门覆盖边界条件，适合反复自动测试。
- 脱敏真实样例由用户提供，用来发现真实排版、措辞和 Word 对象结构中无法预先想到的问题。

只有合成样例容易与真实撰写习惯脱节；只有真实样例又难以覆盖全部边界并难以纳入版本库。因此两者不能完全相互替代。

### 4.2 建议由项目生成的合成样例

后续建议在 `samples/synthetic/` 中至少生成以下文件：

| 样例 | 主要覆盖内容 |
| --- | --- |
| `01_标准机械申请.docx` | 一项独立权利要求、若干从属权利要求、标准附图标记说明和具体实施方式 |
| `02_标号边界.docx` | 名称重叠、重复名称、1/10/100、字母后缀、连字符、小数点、漏分号和同号异名 |
| `03_权利要求转发明内容.docx` | 多项独立权利要求、多段从属权利要求、引用关系、终止标题和异常编号 |
| `04_修订与复杂对象.docx` | 开启修订、已有修订、批注、超链接、域、表格、公式和内容控件 |
| `05_多节页眉.docx` | 首页不同、奇偶页不同、多节、链接到前一节、空页眉和不同文件名格式 |
| `06_格式清理边界.docx` | 中英文标点、引号、反单引号、Markdown、网址、邮箱、英文缩写、单位、编号和混合字体 |

每个样例应同时配一份期望说明，至少记录：

- 要运行的工具。
- 运行前应选择的范围。
- 文档运行前的修订状态。
- 哪些文本或格式应改变。
- 哪些内容必须保持不变。
- 预期修改数量和警告。

当输出可以稳定比较时，可增加 `samples/expected/` 中的期望文档副本；否则使用 Markdown 检查表记录人工验收点。

### 4.3 希望用户提供的真实材料

开始修改业务规则前，建议至少提供以下脱敏材料：

1. 一份日常最常见、格式较规范的机械领域申请文件，用作真实基准。
2. 一至两份曾经触发错误或需要大量人工修正的文件。
3. 如果某个 BUG 只发生在特定文档中，提供该文档的最小化副本以及期望修改结果。

最有价值的是“修改前 + 人工修改后的正确版本”成对样例。若只能提供修改前文件，也应附带文字说明或截图，指出正确结果。

提供真实文件前请移除或替换不应进入仓库的信息，例如申请人、发明人、地址、联系电话、真实案号和未公开技术秘密。脱敏时应尽量保留段落、样式、域、页眉、修订、表格和标点结构，因为这些结构往往正是 BUG 的来源。

真实材料默认不提交 Git。建议存放在 `samples/private/`，并将该目录加入 `.gitignore`；只有确认可公开进入本地仓库的脱敏样例才放入 `samples/real/`。

## 5. BUG 信息的最低要求

每个 BUG 建议按以下格式记录：

```text
工具名称：
Word 版本及 32/64 位：
输入文件或最小样例：
执行前选区：
执行前修订状态：
实际结果：
期望结果：
是否每次可复现：
是否已有人工修正后的文件：
```

“有时标错”不足以直接修改规则。至少需要知道被误处理的原文、应有结果和当时选区，才能把问题变成回归测试。

## 6. 当前代码的重点风险

静态盘点后，后续测试应优先关注以下风险：

- 多个过程直接依赖 `ActiveDocument` 和 `Selection`，难以测试，也可能在用户切换文档后作用到错误目标。
- 多处直接切换 `TrackRevisions`；辅助过程发生错误时，存在修订状态未完全恢复的风险。
- `ScreenUpdating` 和 `StatusBar` 的恢复逻辑分散，应集中到统一清理出口。
- 大量使用 `Range.Text` 或全局查找替换，可能影响字符格式、域、超链接、批注、内容控件或已有修订。
- 标号名称存在包含关系时，例如“支架”和“支架板”，需要明确最长匹配、重复标号和后缀规则。
- 格式清理中的全角标点转换不能误改网址、邮箱、英文缩写、公式、数值、单位和代码片段。
- 多节页眉涉及 `DifferentFirstPageHeaderFooter`、`OddAndEvenPagesHeaderFooter` 和 `LinkToPrevious`，不能只按可见第一页测试。

这些是测试优先级，并不表示已经确认每一项都是现存 BUG。任何修复都应以可复现样例为依据。

## 7. 测试层次与完成标准

### 7.1 纯文本规则测试

尽量把标号解析、名称匹配、权利要求重写和标点判断提取为不依赖 Word 对象的函数。用字符串输入和期望输出测试。这类测试最快，也最适合覆盖大量边界条件。

### 7.2 Word 集成测试

在一次性文档副本上运行宏，检查：

- 正文文本和字符格式。
- 段落、样式、列表编号和表格。
- 修订和批注是否保留。
- 域、超链接、公式和内容控件是否完好。
- 页眉、节和“链接到前一节”的行为。
- 原先的应用状态是否恢复。

### 7.3 人工验收

每个正式版本至少应在一份标准真实样例和一份复杂真实样例上完整运行。验收人应能从报告中看到修改数和警告，而不是只能凭肉眼猜测宏是否做过处理。

### 7.4 质量门槛

一次修改只有同时满足以下条件才算完成：

- 有明确的需求或 BUG 说明。
- 有可重复输入和预期结果。
- 新样例通过。
- 既有标准样例没有回归。
- 出错后文档和 Word 全局状态可恢复。
- 未直接覆盖用户的原始申请文件。
- 源码、文档和发布模板保持一致。

## 8. 源码、构建与部署约定

- `src/` 是可审阅的源码，后续开发以源码为准。
- `dist/PatentTools.dotm` 当前是原始基线，不应在普通开发提交中反复覆盖。
- 新增 UserForm 时必须同时保存 `.frm` 和配套 `.frx` 文件。
- 仓库源码统一使用 UTF-8；VBE 对 UTF-8 和中文过程名的支持不可靠。构建或手工导入前，应生成临时 ANSI/GBK（Windows 代码页 936）副本，导入后再核对六个中文名称及窗体中文标题是否正常。不要为迁就 VBE 而把 Git 中的源码长期改回本地代码页。
- `scripts/prepare_vbe_import.ps1` 已用于生成代码页 936 的临时导入副本；输出在被忽略的 `build/vbe-import/`，不要把该副本当作源码提交。
- 建议增加 `scripts/` 中的导入、导出和构建脚本，但自动操作 VBA 工程通常要求 Word 开启“信任对 VBA 项目对象模型的访问”。项目不得自行修改该安全设置；未开启时采用手工导入方式。
- 发布产物建议使用版本号，例如 `PatentTools-0.2.0.dotm`，并记录对应 Git 提交。
- 正式替换 Word `STARTUP` 中的模板前，必须备份旧模板并关闭所有 Word 进程。
- 如果工具将分发给其他人员，后续应评估 VBA 数字签名、受信任位置和版本升级机制。

建议逐步形成以下目录：

```text
AGENTS.md
README.md
src/
  modToolboxEntry.bas
  modToolDispatcher.bas
  modDocumentContext.bas
  modFigureTagService.bas
  modFormatService.bas
  frmPatentToolbox.frm
  frmPatentToolbox.frx
tests/
samples/
  synthetic/
  expected/
  private/       # 不进入 Git
docs/
  tools/
  bugs/
scripts/
dist/
```

## 9. 建议的近期任务顺序

1. 用户列出当前已知 BUG，并为优先级最高的 BUG 提供最小复现信息。
2. 项目生成六类合成 `.docx` 样例及验收说明。
3. 用户补充至少一份标准脱敏真实文件和一份问题文件。
4. 为六个现有宏记录基线输出。
5. 实现单一入口和第一版工具面板，只调整结构、不改规则。
6. 在基线样例上确认重构前后行为一致。
7. 按优先级逐项修复 BUG，并为每项增加回归样例。
8. 完成集成验收后生成新的版本化 `.dotm`。

## 10. 对后续协作者和 Agent 的约束

- 先读本文件、`README.md` 和相关模块，再修改代码。
- 不把 CAD 英文宏纳入本项目。
- 不直接修改或替换 Word 正在使用的正式模板。
- 不在缺少样例和期望结果时猜测专利撰写规则。
- 结构重构与业务规则修改分开提交。
- 所有会修改全文、关闭修订或影响页眉的功能必须明确提示作用范围。
- 测试只操作副本；任何真实申请文件默认视为敏感材料。
- 发现新的边界条件时，先补样例，再补代码。
